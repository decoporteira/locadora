<%= form_for @genre do |f| %>
  <%= f.label :name, 'Nome' %>
  <%= f.text_field :name %>
  <%= f.submit 'Editar' %>
<% end %>
<%= button_to "Destroy this classroom" , @task,
	method: :delete,method: :delete %>